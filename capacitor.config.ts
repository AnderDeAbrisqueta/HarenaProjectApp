import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'HarenaProjectApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
