import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.harenaprojectapp.app',
  appName: 'HarenaProjectApp',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
