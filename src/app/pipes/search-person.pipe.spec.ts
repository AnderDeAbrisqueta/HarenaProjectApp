import { SearchPersonPipe } from './search-person.pipe';

describe('SearchPersonPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPersonPipe();
    expect(pipe).toBeTruthy();
  });
});
